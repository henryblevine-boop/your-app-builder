## Goal

Keep this project untouched so Claude Code can build the app via GitHub sync.

## What I will do

Nothing to the code. The project stays on the default template (blank placeholder home page, no extra routes, no backend).

## What you need to do

1. In the chat input, open the **Plus (+) menu → GitHub → Connect project**, authorize the Lovable GitHub App, pick the account/org, and create the repository.
2. Clone that repo locally and point Claude Code at it.
3. Pushes to the repo's default branch sync back into Lovable automatically, and anything I change here pushes out to GitHub.

## Notes

- Lovable can't import an existing repo — the repo must be created from this project.
- If the app later needs a database, auth, or server secrets, those are enabled on the Lovable side (Cloud), not through the repo. Tell me when that time comes.
- Until then I'll stay hands-off and only act when you ask.
